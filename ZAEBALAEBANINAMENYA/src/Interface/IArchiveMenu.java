package Interface;

import java.util.List;

/*public interface IArchiveMenu<Archive> {
    void createArchive(String name);
    void deleteArchive(int index);
    void editArchive(int index, String newName);
    String listArchives();
    boolean selectArchive(int index);
    Archive getCurrentArchive();
    //нарисовать интерфейсы
    List<Archive> getArchives();//возвращает список всех архивов
}*/